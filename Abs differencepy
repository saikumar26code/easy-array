import sys
def array(arr):
    result=sys.maxsize
    for i in range(len(arr)):
        sum=0
        for j in range(len(arr)):
            sum+=abs(arr[i]-arr[j])
        result=min(result,sum)
    return result
arr=[1,2,3,4,5]
print(array(arr)) #dont know whether completed or not